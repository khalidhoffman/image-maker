doctype html
html
    head
        title #{error.status || 'Error'}
        link(rel='stylesheet', href='/styles/style.css')
    body
        h1= message
        h2= error.status
        pre #{error.stack}
